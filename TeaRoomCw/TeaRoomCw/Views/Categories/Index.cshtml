@model IEnumerable<TeaRoomCw.Models.Category>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.catName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.catInfo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.catPicFile)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.catName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.catInfo)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.catPicFile)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.catId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.catId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.catId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
